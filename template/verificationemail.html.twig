{% extends "layout.html.twig" %}

{% block title %}
    Page ajouter Contact
{% endblock %}

{% block content %}
<div class="bg-light rounded px-3 py-3">
    <div class="container">
      <h1>Vérification de l'email</h1>
    </div>
</div>
<div class="container mb-5 mt-5">

    {% if form.message is defined %}
        <div class="alert text-center alert-{{ form.state }}" role="alert">
            {{ form.message }}
        </div>
    {% endif %}
</div>

{% endblock %}