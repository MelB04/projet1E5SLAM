{% extends "layout.html.twig" %}

{% block title %}
    Page Modification Contact
{% endblock %}

{% block content %}
<div class="bg-light rounded px-3 py-3">
  <div class="container">
    {% if contactnull == true %}
    <h1>L'élément Contact n'existe plus</h1>
    {% else %}
    <h1>Modifier le Contact n° {{ updateContact.IDContact }}</h1>
    {% endif %}
  </div>
</div>
<div class="container mb-5">   
  {% if contactnull == false %}
  <form action="?page=updateContact&id={{ updateContact.IDContact }}" method="POST">
    <div class="mb-3">
      <label for="Nom du Contact" class="form-label">Nom du Contact</label>
      <input type="nom" class="form-control" id="Nom du Contact" name="Nom du Contact" value="{{ updateContact.IDContact }}">
    </div>
  <button type="submit" class="btn btn-primary" name="btnUpdContrat">Modifier le Contrat n° {{ updateContrat.IDContrat }}</button> <!-- On va vouloir détecter quand on appuie-->
  </form> 
  {% endif %}
</div>
{% endblock %}
